# Phase 2.2 — UI Overhaul + Public UX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Polish both admin and public-facing pages — better visuals, mobile-responsive, government-appropriate color system, and complete missing UX flows.

**Architecture:** Mostly frontend changes. One schema migration (venueHidden). No new API endpoints. All changes are independent UI improvements that can be built in parallel.

**Tech Stack:** Next.js 15 App Router, Tailwind CSS, shadcn/ui, lucide-react icons, Prisma 5

**Note:** Event detail tab layout (Overview/Registrations/Check-in) was already completed in v2.1. Skip that item.

---

## Task 1: Schema — Add venueHidden to Event

**Files:**
- Modify: `prisma/schema.prisma`
- Modify: `prisma/seed.ts`

- [ ] Add `venueHidden Boolean @default(false)` to Event model after `venue` field
- [ ] Update seed data: set `venueHidden: true` on Event 1 (Q2 All-Hands) to demonstrate the feature
- [ ] Run `npx prisma db push` to apply schema change
- [ ] Run `npx prisma db seed` to reseed
- [ ] Verify: `npx prisma studio` or query shows the new field

---

## Task 2: Admin Sidebar — Icons + Polish

**Files:**
- Modify: `components/layout/AdminSidebar.tsx`
- Install: `lucide-react` (if not already installed)

- [ ] Install lucide-react: `npm install lucide-react`
- [ ] Redesign sidebar:
  - Logo area: "GovEvent" with shield icon, subtitle "Admin Portal"
  - Nav links with icons: LayoutDashboard (Dashboard), Calendar (Events), Shield (Blacklist), FileText (Audit Log)
  - Active state: bg-blue-600/20 text-white border-l-3 border-blue-400
  - Inactive: text-gray-400 hover:text-white hover:bg-white/5
  - Bottom: user info section showing session user name + Sign out button
  - Government color palette: dark navy sidebar (bg-slate-900)
- [ ] Verify: sidebar renders correctly, active states work on all 4 routes

---

## Task 3: Public Event Cards — Visual Redesign

**Files:**
- Modify: `components/features/EventCard.tsx`

- [ ] Redesign card layout:
  - Remove emoji icons (📅 📍), use clean text layout
  - Header: title (font-semibold text-lg) + status indicators (FREE badge green, PAID badge with price)
  - Date/time row with formatted date range
  - Venue row (or "Venue revealed upon approval" if venueHidden)
  - Capacity progress bar (Feature E): colored bar + "X/Y seats" text
    - Green (<70%), Amber (70-90%), Red (>90%), "Waitlist open" if full
  - Eligibility note (if domains/depts restricted)
  - CTA button: "Register →" or "Registration Closed" or "Event Full — Join Waitlist"
- [ ] Verify: cards render on /events with correct data

---

## Task 4: Capacity Progress Bar Component

**Files:**
- Create: `components/features/CapacityBar.tsx`

- [ ] Create reusable component:
  - Props: `{ registered: number; capacity: number; showLabel?: boolean }`
  - Visual: rounded-full bar with percentage fill
  - Colors: green (<70%), amber (70-90%), red (>90%)
  - Label: "42 / 60 seats" or "Full — Waitlist open" when >= capacity
- [ ] Use in EventCard (Task 3) and event detail page (Task 5)

---

## Task 5: Public Event Detail — Redesign + Deadline Countdown + Venue Hiding

**Files:**
- Modify: `app/(public)/events/[id]/page.tsx`
- Create: `components/features/DeadlineCountdown.tsx`

- [ ] Create `DeadlineCountdown` client component:
  - Props: `{ deadline: string }` (ISO datetime)
  - Shows: "Registration closes in 2d 4h 30m" with live countdown (useEffect + setInterval)
  - When past: shows "Registration closed" in red
  - When < 24h: show amber warning color
- [ ] Redesign event detail page:
  - Clean card layout with clear sections
  - Title + status badges at top
  - Info grid: Date/Time, Venue (or "Venue revealed upon approval" if venueHidden), Capacity bar, CPD Hours, Price
  - Deadline countdown component
  - Full description section
  - Register CTA button (or closed/full state)
- [ ] Venue hiding: check `event.venueHidden` — if true show placeholder text
- [ ] Query must include `venueHidden` field

---

## Task 6: Registration Form — Success CTA + Eligibility Preview

**Files:**
- Modify: `components/features/RegistrationForm.tsx`

- [ ] Success screen improvements:
  - Show status badge (PENDING / WAITLISTED)
  - "What happens next" section:
    - Free event: "The organiser will review your registration. You'll receive an email when approved."
    - Paid event: "Once approved, you'll receive a payment link via email."
    - Waitlisted: "You're on the waitlist at position X. We'll notify you if a spot opens up."
  - CTA button: "View My Registrations →" linking to `/my-registrations`
- [ ] Real-time eligibility preview:
  - As user types email, check domain against event's allowedDomains
  - Green checkmark if eligible, amber warning if domain doesn't match
  - Debounce 500ms to avoid flicker
  - Props must include `allowedDomains: string[]` (pass from parent page)
- [ ] Update register page to pass allowedDomains to RegistrationForm

---

## Task 7: My Registrations — Payment Button + Type Safety

**Files:**
- Modify: `app/(public)/my-registrations/page.tsx`

- [ ] Fix type safety: replace `type Registration = any` with proper typed interface
- [ ] PENDING_PAYMENT registrations:
  - Show amber "Awaiting Payment" badge (already works via StatusBadge)
  - Add "Pay Now →" button that opens `stripeSessionId` URL (stored as Checkout URL)
  - Show payment deadline if available
- [ ] Venue display: for APPROVED registrations, show venue; for others show "Venue details available after approval" if event.venueHidden
- [ ] CPD hours summary at top: "Total CPD Hours: X.X" across all ATTENDED registrations
- [ ] API response must include `stripeSessionId` and event details — check `/api/my-registrations` route

---

## Task 8: Mobile Responsive — Public Pages

**Files:**
- Modify: `components/layout/PublicNav.tsx`
- Modify: `app/(public)/layout.tsx`
- Modify: `app/(public)/events/page.tsx`

- [ ] PublicNav mobile:
  - Hamburger menu icon on small screens (md:hidden)
  - Slide-down or dropdown nav on mobile
  - Logo + nav links stack on mobile
- [ ] Events listing: cards already full-width, verify spacing on small screens
- [ ] Event detail: info grid should stack to single column on mobile
- [ ] Registration form: already max-w-lg, verify it works on mobile
- [ ] My Registrations: cards should be full-width on mobile

---

## Task 9: Loading Skeletons

**Files:**
- Create: `app/(public)/events/loading.tsx`
- Create: `app/(public)/events/[id]/loading.tsx`
- Create: `app/admin/(protected)/events/loading.tsx`
- Create: `app/admin/(protected)/audit-log/loading.tsx`

- [ ] Create skeleton components for each page using Next.js loading.tsx convention:
  - Events listing: 3 skeleton cards with pulsing rectangles
  - Event detail: skeleton header + info grid + description block
  - Admin events: skeleton table rows
  - Admin audit log: skeleton card rows
- [ ] Use Tailwind `animate-pulse bg-gray-200 rounded` pattern

---

## Task 10: EventOverview — venueHidden Toggle

**Files:**
- Modify: `components/features/admin/EventOverview.tsx`
- Modify: `components/features/admin/EventForm.tsx` (create event form)

- [ ] Add `venueHidden` field to EventOverview FIELDS config (checkbox type, label "Hide Venue Until Approved")
- [ ] Add `venueHidden` toggle to EventForm (create new event form)
- [ ] EventOverview EventData interface: add `venueHidden: boolean`
- [ ] Event detail page: pass `venueHidden` to EventOverview

---

## Task 11: Admin Events List — Polish

**Files:**
- Modify: `app/admin/(protected)/events/page.tsx`

- [ ] Improve table styling:
  - Date column: show formatted date + time, not just date
  - Add capacity column: "X / Y" with color indicator
  - Status badge: use consistent badge component
  - Title clickable (entire row or title links to detail)
  - Remove separate "Manage" text link — title IS the link
  - Add "Check-in" as icon button instead of text
- [ ] Add empty state when no events exist

---

## Task 12: Admin Dashboard — Polish

**Files:**
- Modify: `app/admin/(protected)/page.tsx`

- [ ] Improve stat cards:
  - Add icons (Calendar, Clock, UserCheck from lucide-react)
  - Better card design: icon left, number + label right
  - Add a 4th card: "Total Registrations" or "Events This Week"
- [ ] Add quick-action links below stats: "Create Event", "View Audit Log"

---

## Task 13: Public Layout — Government Color System

**Files:**
- Modify: `app/(public)/layout.tsx`
- Modify: `components/layout/PublicNav.tsx`
- Modify: `app/globals.css` (if needed)

- [ ] Apply government-appropriate color system:
  - Primary: navy blue (#1e3a5f or similar)
  - Nav: white background with navy text, blue accent on active
  - Page background: light gray (bg-gray-50, already set)
  - Accent: blue-600 for CTAs, green for success, amber for warnings
- [ ] PublicNav redesign:
  - Taller nav (h-16) with better spacing
  - "GovEvent" logo text in navy bold + optional shield icon
  - Right side: "My Registrations" as a styled button/link
  - Bottom border or subtle shadow for depth

---

## Task 14: Clean Up Legacy Files

**Files:**
- Delete or update: `components/features/admin/AuditTimeline.tsx`
- Delete or update: `app/admin/(protected)/events/[id]/registrations/page.tsx`
- Delete or update: `app/admin/(protected)/checkin/[id]/page.tsx`

- [ ] AuditTimeline.tsx: verify it's unused (replaced by AuditLogList), delete if unused
- [ ] Registrations page: now embedded in event detail tabs — redirect to `/admin/events/[id]` or keep as fallback
- [ ] Checkin page: now embedded in event detail tabs — redirect to `/admin/events/[id]` or keep as fallback

---

## Task 15: Verify + Build

- [ ] Run `npx tsc --noEmit` — 0 errors
- [ ] Run `npm run build` — 0 errors
- [ ] Manual smoke test all pages
